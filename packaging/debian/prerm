#!/bin/sh
# prerm for leviculum. Service teardown is auto-injected by cargo-deb
# from the systemd-units metadata (the marker below). Note: cargo-deb
# substitutes the marker as plain text wherever it appears, including
# inside comments — so we never spell it out twice in the same file.

set -e

#DEBHELPER#

exit 0
