[package]
name = "leviculum-rx-turnaround"
version = "0.1.0"
edition = "2021"
authors = ["Lew Palm"]
license = "AGPL-3.0-or-later"
description = "Pure defer-or-proceed decision for the LoRa RX->TX turnaround, host-testable"

[dependencies]
leviculum-core = { path = "../../leviculum-core", default-features = false }
