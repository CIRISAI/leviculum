[package]
name = "leviculum-channel-access"
version = "0.1.0"
edition = "2021"
authors = ["Lew Palm"]
license = "AGPL-3.0-or-later"
description = "Pure LoRa channel-access policy for the firmware's transmit path: acquisition jitter and the CAD retry gate, host-testable"

[dependencies]
