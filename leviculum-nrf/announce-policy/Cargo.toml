[package]
name = "leviculum-announce-policy"
version = "0.1.0"
edition = "2021"
authors = ["Lew Palm"]
license = "AGPL-3.0-or-later"
description = "Pure announce cadence and per-peer rate limit for both leviculum stacks, host-testable"

[dependencies]
# The movement thresholds and the displacement test live there, once
# (#401): min_distance_m, max_hdop_e2, settle_ms and `moved_at_least`.
leviculum-telemetry-policy = { path = "../telemetry-policy" }
