//! `lblogd` binary stub. The post model and renderers live in the library;
//! the HTTP server and the Reticulum node are wired up in a later batch.

fn main() {
    println!("lblogd: not yet wired (batch A)");
}
